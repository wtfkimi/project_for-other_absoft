pipeline {
    agent {
        label 'master'
     }
    stages {
        stage('build') {
            steps {
                sh 'ls -la'
            }
            steps{
                sh '. .venv_vens/bin/activate'
            }
        }
    }
}
