pipeline {
    agent {
        label 'master'
     }
    stages {
        stage('build') {
            steps {
                sh 'ls -la'
            }

        stage('build'){
            steps{
                sh '. .venv_vens/bin/activate'
            }
        }
        }
    }
}
